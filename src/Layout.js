import React from "react";
import { Grid } from "@mui/material";

const Layout = () => {
  return (
    <Grid container spacing={3}>
      <Grid item xs={3} md={2.4} lg={2}></Grid>
    </Grid>
  );
};

export default Layout;
